#include <iostream>
#include <string.h>
using namespace std;
typedef long long ll;

ll dp[1001][29][29];
int main() {
	ios::sync_with_stdio(0), cin.tie(0), cout.tie(0);
	int t; cin >> t;
	while (t--) {
		int y, x,c; cin >> y >> x >> c;
		for (int i = 0; i < c; i++) {
			int b, a; cin >> b >> a;
		}

	}
}